// Simple amount to words function
export function amountToWords(amount: number, currency: string = 'AED'): string {
  // Placeholder - implement proper conversion
  return `${currency} ${amount.toFixed(2)}`;
}